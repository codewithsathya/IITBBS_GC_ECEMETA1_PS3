module.exports = (io) => {
    const createOrder = function(payload){
        const socket = this
    }

    const readOrder = function(orderId, callback){

    }

    return {
        createOrder,
        readOrder
    }
}